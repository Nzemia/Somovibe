version: "3.9"
services:
  app:
    build:
      context: .
      args:
        DATABASE_URL: "${DATABASE_URL}"
    image: questy:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
